# Tomcat
server:
  port: 19308

# mybatis配置
mybatis:   
  # 搜索指定包别名
  typeAliasesPackage: com.mqttsnet.thinglinks.rule
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath:mapper/**/*.xml

# swagger配置
swagger:
  title: Link模块接口文档
  license: Powered By thinglinks
  licenseUrl: https://showdoc.thinglinks.mqttsnet.com