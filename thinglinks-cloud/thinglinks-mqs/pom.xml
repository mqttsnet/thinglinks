<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.mqttsnet.thinglinks</groupId>
        <artifactId>thinglinks-dependencies-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../thinglinks-dependencies-parent/pom.xml</relativePath>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.mqttsnet.thinglinks</groupId>
    <artifactId>thinglinks-mqs</artifactId>
    <name>${project.artifactId}</name>
    <description>消息治理服务</description>
    <packaging>pom</packaging>

    <modules>
        <module>thinglinks-mqs-facade</module>
        <module>thinglinks-mqs-server</module>
        <module>thinglinks-mqs-entity</module>
        <module>thinglinks-mqs-biz</module>
        <module>thinglinks-mqs-biz-bus</module>
        <module>thinglinks-mqs-controller</module>
    </modules>
</project>
