<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>thinglinks-mqs</artifactId>
        <groupId>com.mqttsnet.thinglinks</groupId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>thinglinks-mqs-entity</artifactId>
    <name>${project.artifactId}</name>
    <description>消息治理服务-实体模块</description>

    <dependencies>
        <dependency>
            <groupId>com.mqttsnet.thinglinks</groupId>
            <artifactId>thinglinks-common</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- 复用 DeviceActionTypeEnum 等 link 域共享枚举(协议事件类型已统一收敛到 link-entity) -->
        <dependency>
            <groupId>com.mqttsnet.thinglinks</groupId>
            <artifactId>thinglinks-link-entity</artifactId>
            <version>${revision}</version>
        </dependency>
    </dependencies>

</project>
